services:
  app:
    build: .
    ports:
      - "8100:8100"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: ionic serve --host=0.0.0.0 --no-open
